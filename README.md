This project is a React Router v6 practice exercise.  
It demonstrates:

- Basic navigation using `<Link>`
- Setting up routes with `<Routes>` and `<Route>`
- Nested routes
- Dynamic route parameters using `:slug`
- Rendering the correct blog post using `useParams()`

---

## 🚀 How to Run the Project

```bash
npm install
npm start
